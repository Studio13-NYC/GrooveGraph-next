---
description: MTA / NYCTA design foundation for GrooveGraph Next (Vignelli map + manual extract).
alwaysApply: true
---

# MTA design foundation

- **`docs/design-language/FOUNDATION.mdc`** — merged authority for NYCTA / Vignelli–aligned work (map grammar, signage discipline, software wayfinding; formerly split across removed asset-folder extracts)
- **Tokens:** `product/src/visual-system/nycta-groovegraph-tokens.css` — imported from `product/app/globals.css` via `@import "../src/visual-system/nycta-groovegraph-tokens.css";`
- **Historical scans / extracts:** were under `graphic-design-agent-assets/` (removed) — recover from **git history** if needed
- Map grammar: orthogonal + 45°, uniform line weight, parallel gutters, repeatable radii, explicit interchanges.
- Directional UI accent = `--gg-accent-directional` (NYCTA orange route).
