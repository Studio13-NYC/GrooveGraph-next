# Logs

Place shareable debug or instrument runs here (for example NDJSON lines from browser or API diagnostics).

- Root `.gitignore` ignores `*.log`; specific files can be allowlisted when they are safe reference material (no tokens, cookies, or connection strings with credentials).
- Prefer redacted copies for anything that might contain PII or secrets.
