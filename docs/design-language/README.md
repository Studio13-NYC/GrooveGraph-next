# Design language (GrooveGraph Next)

**Start here:** [`FOUNDATION.mdc`](./FOUNDATION.mdc) — full merged authority (Vignelli map + NYCTA manual extract + software wayfinding).

**Pencil design system:** [`GrooveGraph-design_system-pencil.pen`](./GrooveGraph-design_system-pencil.pen) — includes a **Design system — library** artboard (id `dsKitLib`) with route color swatches, radius scale, typography ladder, and **10 reusable components** (`Button / Primary`, `Outline`, `Link`, `Sign band`, `Plate / Masthead`, `Badge`, `Decision row`, `Field`, `Alert / Info rail`, `Route chip`). Open in Pencil to instance components on new frames.

**Quick entry:** [`../VISUAL_STYLE_GUIDE.mdc`](../VISUAL_STYLE_GUIDE.mdc) — summary, regimes, checklist.

**CSS tokens:** `framework/src/visual-system/nycta-groovegraph-tokens.css` — import `@groovegraph-next/framework/nycta-groovegraph-tokens.css` from any app; re-export beside map scans in `MTA-Graphic_Deisgn_Standards/`

**Archive index:** `graphic-design-agent-assets/MTA-Graphic_Deisgn_Standards/CANONICAL.md`
