# Framework

This directory is the first implementation target for the clean-start GrooveGraph framework.

It will hold reusable orchestration helpers, packet tooling, and any support code that should exist before the future `product/` implementation begins.
