# Product scripts

Neo4j introspection helpers that lived here are archived under **`archive/neo4j-branch-only/product/scripts/`** (see that folder’s README). The live app uses **TypeDB only** for graph sync.
