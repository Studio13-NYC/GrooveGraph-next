# Visual system (product)

Operational visual primitives for the shipped Next app.

## Contents

- **`nycta-groovegraph-tokens.css`** — canonical NYCTA / Vignelli CSS custom properties (imported from `app/globals.css`).
- **`tokens.ts`** — TypeScript mirror of palette, typography, and geometry; keep in sync with the CSS file.

Design authority: `docs/design-language/FOUNDATION.mdc`.
